services:
  app:
    build: .
    ports:
      - "127.0.0.1:4000:4000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - SERVER_HOST=0.0.0.0
      - DATA_DIR=/app/data
      - CLIENT_ORIGIN=http://localhost:4000
    volumes:
      - ./data:/app/data
