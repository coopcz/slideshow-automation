services:
  app:
    build: .
    ports:
      - "4000:4000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - DATA_DIR=/app/data
      - CLIENT_ORIGIN=http://localhost:4000
    volumes:
      - ./data:/app/data
